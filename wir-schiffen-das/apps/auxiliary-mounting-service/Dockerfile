# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build auxiliary-mounting-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t auxiliary-mounting-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0


WORKDIR /app

RUN addgroup --system auxiliary-mounting-service && \
    adduser --system -G auxiliary-mounting-service auxiliary-mounting-service

COPY dist/apps/auxiliary-mounting-service auxiliary-mounting-service
RUN chown -R auxiliary-mounting-service:auxiliary-mounting-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix auxiliary-mounting-service --omit=dev -f install

CMD [ "node", "auxiliary-mounting-service" ]
