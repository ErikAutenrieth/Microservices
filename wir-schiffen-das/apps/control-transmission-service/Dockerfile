# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build control-transmission-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t control-transmission-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0


WORKDIR /app

RUN addgroup --system control-transmission-service && \
          adduser --system -G control-transmission-service control-transmission-service

COPY dist/apps/control-transmission-service control-transmission-service
RUN chown -R control-transmission-service:control-transmission-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix control-transmission-service --omit=dev -f install

CMD [ "node", "control-transmission-service" ]
