# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build cooling-exhaust-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t cooling-exhaust-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system cooling-exhaust-service && \
          adduser --system -G cooling-exhaust-service cooling-exhaust-service

COPY dist/apps/cooling-exhaust-service cooling-exhaust-service
RUN chown -R cooling-exhaust-service:cooling-exhaust-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix cooling-exhaust-service --omit=dev -f install

CMD [ "node", "cooling-exhaust-service" ]
